procs:
  backend:
    shell: "uv run uvicorn main:app --reload"
    cwd: "backend"
  frontend:
    shell: "npm run dev"
    cwd: "frontend"
  frontend-demo:
    shell: "npm run dev"
    cwd: "frontend-demo"
